# Provenance

Single source.

## https://github.com/obra/superpowers, version 6.3.0, path skills/using-git-worktrees/SKILL.md

Taken: the whole skill: the core principle, Step 0 isolation detection with the submodule guard and the consent question, Step 1 native tools first then the git worktree fallback with directory selection, the ignore check and the sandbox fallback, Step 2 project setup, Step 3 baseline tests and the report, the quick reference table, and the common rationalizations table. The `.worktrees/` default directory is kept.
Changed: renamed to worktree; description rewritten trigger-first; the "Announce at start" line removed; "your human partner" became "the user"; em dashes rewritten as periods, commas, or colons; headings set in sentence case.

## Tier

model-invoked (tier 2): called mid-flow by execute at setup; also typed by the user.
