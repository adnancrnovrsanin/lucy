# Provenance

Single source.

## https://github.com/mattpocock/skills, commit 6654f6b60cd9d5be8b54c6fafe44346dabeb3b76, path skills/productivity/writing-for-agents

Taken: the whole SKILL.md body: context pointers, the two loads, the information hierarchy, steps and completion criteria, when to split, leading words, and pruning; SKILL-MECHANICS.md whole (invocation, splitting by invocation, router skills).
Changed: the description, rewritten trigger-first with dispatching a subagent or writing its brief added as a trigger; one paragraph added at the end of the "Context pointers" section stating that a dispatch prompt is a document for an agent; one sentence added after the first paragraph of SKILL-MECHANICS.md stating Lucy's user-invoked default; agents/openai.yaml, regenerated.

## Tier

model-invoked (tier 2): the model must apply it unasked when dispatching a subagent or writing agent documents.
