# Provenance

Single source.

## https://github.com/mattpocock/skills, commit 6654f6b60cd9d5be8b54c6fafe44346dabeb3b76, path skills/engineering/wizard

Taken: SKILL.md whole (what a wizard is, the four-step process: scope, map, author, verify and hand off) and template.sh verbatim, including its example stage.
Changed: description shortened to the human-only trigger; disable-model-invocation: true added, so the skill loads only when typed; unslop pass over SKILL.md (prohibitions rewritten as instructions, colon connectors split, "delightful" cut); template.sh untouched, since the skill itself says the library above the STAGES marker is never hand-edited; agents/openai.yaml regenerated with the policy block; LICENSE and this file added.

## Tier

user-invoked (tier 3): typed by the user; revisit after real use, upstream ships it model-invoked.
