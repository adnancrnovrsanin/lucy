# Provenance

Merged skill.

## https://github.com/obra/superpowers, version 6.3.0, path skills/test-driven-development

Taken: the overview, when to use, the loop (red, verify red, green, verify green, refactor, repeat) with its TypeScript examples, the good tests table, the when stuck table, the rationalizations table, the bug fix example, the final rule, and writing-good-tests.md as the base of the reference file.
Changed: glyphs and emoji replaced with words; the dot graph dropped, with one sentence of prose carrying its edges; the rationalizations table trimmed to six rows; the iron law, red flags, verification checklist and debugging integration sections dropped, their substance kept in the rationalizations table and the rule; "your human partner" replaced with "the user"; the writing-skills pointer in writing-good-tests.md replaced with a plain mention of writing-for-agents; em dashes removed; headings set in sentence case.

## https://github.com/mattpocock/skills, commit 6654f6b60cd9d5be8b54c6fafe44346dabeb3b76, path skills/engineering/tdd

Taken: the CONTEXT.md sentence, the seams section with its codebase-design pointer, the three anti-patterns, the refactoring rule, tests.md and mocking.md.
Changed: tests.md and mocking.md merged into writing-good-tests.md as the sections "Examples of tests at seams" and "Mocking"; the tautological-test example from tests.md dropped because the reference file already states that rule under "Derive expectations independently"; the call-count red flag narrowed to internal collaborators so it agrees with "Make doubles specific"; arrows replaced with words; the code-review pointer in the refactoring rule replaced with the word "review"; "Don't mock" list renamed "Keep real".

## Tier

model-invoked (tier 2): the model must apply it unasked whenever it writes implementation code.
