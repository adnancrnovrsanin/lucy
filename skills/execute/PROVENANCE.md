# Provenance

Single source.

## https://github.com/obra/superpowers, version 6.3.0, path skills/subagent-driven-development and skills/executing-plans

Taken: everything from subagent-driven-development: the overview with continuous execution, rulings instead of stalls, and the four stop conditions; setup with the per-plan workspace, the ledger and its recovery rules, the spec as binding authority, and the pre-flight conflict scan; model selection; the task loop (batching same-shape work, waiting on subagents, dispatch, report handling, task review, the five-round fix loop with the breaker, completion); the final whole-branch review with its single fix wave; finish with the exhaustive rulings list; the common rationalizations table; the three prompt templates (implementer, task reviewer, scoped re-review); the three scripts (sdd-workspace, task-brief, review-package). From executing-plans: the inline task loop (follow each step exactly, run every verification, stop at blockers, revisit the plan when the user updates it, finish at the end).
Changed: renamed to execute, user-invoked with a plan path argument; the workspace re-pathed from .superpowers/sdd/ to .lucy/runs/ and plans from docs/superpowers/plans/ to .lucy/plans/, with sdd-workspace appending .lucy/ to the repo's root .gitignore when .lucy is not ignored, instead of writing a self-ignoring .gitignore inside the workspace; the ledger's first line and the ruling, fix-round, and parked line formats rewritten without em dashes (`Ruling:` stays the marker promote reads); upstream skill names became Lucy names as Skill tool calls (worktree at setup, review-changes with its code-reviewer.md for the final review, finish at the end) and the implementer template names the tdd and verify skills; the task loop opens with a writing-for-agents pointer; setup reads the plan's `## Contract inventory` into the pre-flight table; completion requires ALL MET on a task's Gates: ledger through unlazy (execute drives, unlazy proves), with a matching rationalizations row and an unmet gate routed into the fix loop; the workspace is no longer deleted whole at the end, briefs, reports, and review packages go and progress.md stays because promote reads its rulings; executing-plans folded in as the inline mode for sessions without a subagent tool, minus its note that superpowers works better with subagents and its announce line; the two dot graphs (when to use, process) replaced by prose; the example workflow transcript dropped; "your human partner" became "the user" and "the human decides" became "the user decides"; emoji verdict marks (check, cross, warning) became the words "spec compliant", "issues found", and "cannot verify from diff"; em dashes, curly quotes, and Title Case headings brought to Lucy's prose rules.

## Tier

user-invoked (tier 3): typed by the user with a plan path.
