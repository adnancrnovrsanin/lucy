# Provenance

Authored for Lucy on 2026-09-02.

## Shapes borrowed

None copied. Step 3 reads the rulings that execute records in its ledger, the as-built correction shape from the superpowers skill subagent-driven-development. Step 4 applies the ADR triple test (hard to reverse, surprising without context, the result of a real trade-off) from mattpocock/skills domain-modeling. Both are reached through Lucy skills rather than copied text: execute writes the rulings this skill reads, and domain-modeling is a Skill tool call.

## Tier

user-invoked (tier 3): typed by the user after finish; finish reminds, never invokes.
